<!--(a)model IEnumerable<Item>

<div class="container">

<table class='table table-striped'>
<tr>
<th>Id</th>
<th>Name</th>
<th>Price</th>
<th>Description</th>
<th>Image</th>
<th>Actions</th>
</tr>

(a)foreach (var item in Model)
{
    <tr>
        <td>(a)item.ItemId</td>
        <td>
            <a asp-controller="Item"
            asp-action="Details"
            asp-route-id="(a)item.ItemId">(a)item.Name</a>
    </td>
    <td>(a)item.Price.ToString("0.00 NOK")</td>
    <td>(a)item.Description</td>
    <td>
    <img src="(a)item.ImageUrl" alt="imgitem" style="max-width: 80px ; max-height: 80px"/>
    </td>

    <td>
        <a asp-action="Update" asp-route-id="(a)item.ItemId">Update</a>
        <a asp-action="Delete" asp-route-id="(a)item.ItemId">Delete</a>
    </td>
    </tr>
}
</table>

<p>
<a class="btn btn-secondary"
asp-controller="Item"
asp-action="Create">Create new Item</a>
</p>
</div>-->